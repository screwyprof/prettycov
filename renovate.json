{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":semanticCommitTypeAll(chore)"
  ],
  "timezone": "Europe/London",
  "schedule": ["before 6am on monday"],
  "prConcurrentLimit": 3,

  "packageRules": [
    {
      "description": "Automerge everything that is not a major. The gate is make check, which CI runs as one job and branch protection requires; a bump that breaks anything cannot reach main, and a bump that breaks nothing does not need a human to look at it.",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin"],
      "automerge": true,
      "automergeType": "pr",
      "platformAutomerge": true
    },
    {
      "description": "A major is a changelog to read, not a number to bump.",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["major"]
    },
    {
      "description": "One pull request for the Go module set rather than five, since x/ tags together.",
      "matchManagers": ["gomod"],
      "groupName": "go modules"
    },
    {
      "description": "The Makefile's tool pins, grouped: they are one toolchain and make check is one gate over all of them.",
      "matchManagers": ["custom.regex"],
      "groupName": "make tools"
    },
    {
      "description": "Go itself is pinned to 1.26 deliberately — golang/go#80974 makes 1.27 miscount statements, which is the one bug a coverage tool cannot ship on. CL 819000 fixed it for 1.28 with no 1.27 backport, so this lifts when 1.28 ships and not before. The pin lives in go.mod, flake.nix and .github/workflows/go.yml; unblock all three together.",
      "matchDepNames": ["go", "golang", "pkgs.go_1_26"],
      "enabled": false
    }
  ],

  "customManagers": [
    {
      "description": "The Makefile's *_VERSION pins. Every tool is fetched with `go run pkg@version`, so the version lives beside the command that uses it rather than in go.mod — which is why no bot read them until this.",
      "customType": "regex",
      "managerFilePatterns": ["/^Makefile$/"],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>[a-z-]+?)\\s+depName=(?<depName>[^\\s]+?)\\s*\\n\\s*[A-Z_]+_VERSION\\s*:?=\\s*(?<currentValue>\\S+)"
      ]
    }
  ],

  "nix": {
    "description": "flake.lock's nixpkgs input. The devShell is the only thing the flake builds, so this moves the contributor toolchain and nothing a user installs.",
    "enabled": true
  },

  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["before 6am on the first day of the month"]
  },

  "vulnerabilityAlerts": {
    "description": "A security fix does not wait for Monday.",
    "schedule": ["at any time"],
    "automerge": true
  }
}
